VITE_BLOG_PUBLIC_URL=https://www.tradingsignals.ai

# --- Vercel client deploy (recommended): use same-origin proxy in vercel.json ---
VITE_API_BASE_URL=/api

# --- Direct cross-origin API (needs server CORS + disable Deployment Protection on server) ---
# VITE_API_BASE_URL=https://your-server.vercel.app/api

# Only for direct cross-origin + protected preview URLs (does NOT apply to OPTIONS preflight)
# VITE_VERCEL_PROTECTION_BYPASS=
